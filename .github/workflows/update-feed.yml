- name: Generate RSS feed
  run: |
    python -c "
    import requests
    from bs4 import BeautifulSoup
    from feedgen.feed import FeedGenerator
    from datetime import datetime
    import pytz
    import re
    
    url = 'https://carnegieendowment.org/russia-eurasia/politika?lang=ru&center=russia-eurasia'
    headers = {
        'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36',
        'Accept-Language': 'ru-RU,ru;q=0.9,en-US;q=0.8,en;q=0.7'
    }
    
    try:
        print('📡 Lade Carnegie Seite...')
        response = requests.get(url, headers=headers, timeout=20)
        response.raise_for_status()
        print('✅ Seite geladen')
    except Exception as e:
        print(f'❌ Fehler beim Laden: {e}')
        exit(1)
    
    soup = BeautifulSoup(response.content, 'html.parser')
    
    # DEBUG: Seite speichern zur Analyse
    with open('debug_page.html', 'w', encoding='utf-8') as f:
        f.write(soup.prettify())
    print('📄 Debug-HTML gespeichert')
    
    # RSS-Feed konfigurieren
    fg = FeedGenerator()
    fg.title('Carnegie Russia-Eurasia - Politika')
    fg.description('Аналитика Carnegie Russia-Eurasia')
    fg.link(href=url, rel='alternate')
    fg.language('ru')
    
    # VERSCHIEDENE METHODEN ARTIKEL ZU FINDEN
    
    articles_found = []
    
    # Methode 1: Suche nach allen Links zu Artikeln
    print('🔍 Suche nach Artikel-Links...')
    all_links = soup.find_all('a', href=True)
    
    for link in all_links:
        href = link['href']
        text = link.get_text().strip()
        
        # Filter für Artikel-Links
        if '/politika/' in href and len(text) > 20:
            if not href.startswith('http'):
                href = 'https://carnegieendowment.org' + href
            
            # Titel extrahieren
            title = text
            # Versuche besseren Titel zu finden
            parent = link.find_parent(['div', 'article', 'section'])
            if parent:
                heading = parent.find(['h2', 'h3', 'h4', 'h5'])
                if heading:
                    title = heading.get_text().strip()
            
            if title and href:
                articles_found.append((title, href))
                print(f'   Found: {title[:50]}...')
    
    # Methode 2: Suche nach Überschriften
    if not articles_found:
        print('🔍 Suche nach Überschriften...')
        headings = soup.find_all(['h2', 'h3', 'h4', 'h5'])
        
        for heading in headings:
            title = heading.get_text().strip()
            if len(title) > 20:
                # Finde den nächstgelegenen Link
                link = heading.find_parent('a') or heading.find_next('a')
                if link and link.get('href'):
                    href = link['href']
                    if '/politika/' in href:
                        if not href.startswith('http'):
                            href = 'https://carnegieendowment.org' + href
                        articles_found.append((title, href))
                        print(f'   Found via heading: {title[:50]}...')
    
    # Methode 3: Suche nach Content-Containern
    if not articles_found:
        print('🔍 Suche nach Content-Containern...')
        containers = soup.find_all(['article', 'div', 'section'], class_=True)
        
        for container in containers:
            classes = ' '.join(container.get('class', []))
            if any(word in classes.lower() for word in ['card', 'item', 'post', 'article', 'teaser']):
                # Finde Titel und Link
                title_elem = container.find(['h2', 'h3', 'h4', 'h5'])
                link_elem = container.find('a', href=True)
                
                if title_elem and link_elem:
                    title = title_elem.get_text().strip()
                    href = link_elem['href']
                    
                    if '/politika/' in href and len(title) > 20:
                        if not href.startswith('http'):
                            href = 'https://carnegieendowment.org' + href
                        articles_found.append((title, href))
                        print(f'   Found in container: {title[:50]}...')
    
    # Duplikate entfernen
    unique_articles = {}
    for title, href in articles_found:
        unique_articles[href] = title
    
    print(f'📊 Gefundene Artikel: {len(unique_articles)}')
    
    # Artikel zum Feed hinzufügen
    for href, title in list(unique_articles.items())[:15]:
        fe = fg.add_entry()
        fe.title(title)
        fe.link(href=href)
        fe.description(f'Анализ Carnegie: {title}')
        fe.pubDate(datetime.now(pytz.utc))
        print(f'   Added to feed: {title[:50]}...')
    
    # Feed speichern
    fg.rss_file('carnegie_feed.xml', pretty=True)
    
    if len(unique_articles) > 0:
        print(f'✅ Feed erfolgreich mit {len(unique_articles)} Artikeln erstellt')
    else:
        print('❌ Keine Artikel gefunden - Seitenstruktur könnte geändert haben')
        print('💡 Debug-HTML gespeichert als debug_page.html')
    "
